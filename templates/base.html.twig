<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}KINÉCHEZVOUS
			{% endblock %}
		</title>
		<link rel="icon" href="./assets/styles/images/logo_head_ico.ico">
		{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
		<link rel="stylesheet" href="{{ ('./assets/styles/kcv.css') }}">
		{% block stylesheets %}
			{# {{ encore_entry_link_tags('app') }} #}
		{% endblock %}

		{% block javascripts %}
			{# {{ encore_entry_script_tags('app') }} #}
		{% endblock %}
	</head>
	<body>
		<nav>
			<ul>
				<li>
					<a href="#">Section 1</a>
				</li>
				<li>
					<a href="#">Section 2</a>
				</li>
				<li>
					<a href="{{ path ('afficher_calendrier') }}">Calendrier</a>
				</li>

				{% if app.user %}
					<li>
						<p>{{ app.user.nom ~ "," ~ app.user.prenom }}</p>
					</li>
				{% endif %}

				<li>
					<a href="{{ path('app_logout') }}">Logout</a>
				</li>

			</ul>
		</nav>
		{% block body %}{% endblock %}
	</body>
</html>
